<ul>
<% @songs.each do |song| %>
  <% if song.artist %>
    <li><%= link_to song.artist.name, song_path(song.artist) %></li>
  <% else %>
    <li><%= link_to "Add Artist", edit_song_path(song) %></li>
  <% end %>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
