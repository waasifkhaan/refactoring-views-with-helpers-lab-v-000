<h1><%= link_to @song.title, song_path(@song) %></h1>
<% if @song.artist %>
<%= link_to @song.artist_name, artist_path(@song.artist) %>
<%else%>
<%=link_to 'Add artist to song', songs

<%= link_to "All Songs", songs_path %>
